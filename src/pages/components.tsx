
import React, { ReactNode } from 'react';

interface Props {
    children?: ReactNode;

}

/**
 *
 */
export const Components: (props: Props) => React.ReactElement = () => {
  
    return <><h1>Components</h1></>;
};
  