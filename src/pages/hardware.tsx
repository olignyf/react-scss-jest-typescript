
import React, { ReactNode } from 'react';

interface Props {
    children?: ReactNode;

}

/**
 *
 */
export const Hardware: (props: Props) => React.ReactElement = () => {
  
    return <><h1>Hardware</h1></>;
};
  