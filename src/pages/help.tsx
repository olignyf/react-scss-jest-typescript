
import React, { ReactNode } from 'react';

interface Props {
    children?: ReactNode;
}

/**
 *
 */
export const Help: (props: Props) => React.ReactElement = () => {
  
    return <><h1>Help</h1></>;
};
  